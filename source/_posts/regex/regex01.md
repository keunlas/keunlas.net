---
title: 正则前瞻匹配（零宽度先行断言）
tags:
  - 正则表达式
categories:
  - 开发知识
comments: true
abbrlink: 92f1340d
date: 2026-03-11 21:31:16
---

> 基于 ECMAScript 正则表达式语法

断言匹配条件不消耗输入中的任何字符，可参考 `^` `$`，只断言是文本开头或者结尾，不消耗任何字符。

# 零宽度正向先行断言

* `(?=...)` 当 ... 能在当前位置匹配输入，则匹配。

例如对 `baaba` 匹配 `(?=(a+))a*b`，匹配过程如下。

- 开头的 `b` 不符合 `a+`, 进入到下一个字符 `a`.
- `aa` 匹配 `a+`，不消耗字符，继续从这里匹配。
- `aaba` 匹配 `a*b`，匹配完成。
- 最终匹配结果为 `aaba`.

再来看一个例子，对 `abc2ABC` 匹配 `(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9]).{6,}`. 这个正则是匹配包含大写字母、小写字母和数字，且长度至少为 6 的字符串。

首先看第一个前瞻匹配 `(?=.*[a-z])`，用来判断是否包含小写英文字母。

- `.*` 遵守贪心原则，匹配上了 `abc2ABC` 这七个字符, 导致 `[a-z]` 匹配失败。
- `.*` 回溯后匹配 abc2AB 这六个字符，依旧 `[a-z]` 匹配失败。
- 以此类推当 `.*` 回溯到只匹配 `ab` 这两个字符时， `[a-z]` 匹配 `c` 成功。

后续的 `(?=.*[A-Z])(?=.*[0-9])` 和第一个前瞻匹配的原理是一样的。最后的 `.{6,}` 则是匹配至少 `6` 个字符，从而完成判断包含大写字母、小写字母和数字，且长度至少为 6 的字符串。

# 零宽度负向先行断言

* `(?!...)` 当 ... 不能在当前位置匹配输入，则匹配。


零宽度负向先行断言和正向的相反，类比零宽度正向先行断言即可。

