---
title: 正则分组子表达式
tags:
  - 正则表达式
categories:
  - 开发知识
comments: true
abbrlink: 7357b855
date: 2026-03-11 22:21:33
---

> 基于 ECMAScript 正则表达式语法

一对括号就是一对分组，废话不多说，直接看例子。

对 `aabacaabaa` 匹配 `(a+)b\1`，可以匹配到的地方有两处 `aba` 和 `aabaa`。

其中 `\1` 指第一个分组所匹配到的东西。当匹配到第二个字符时，`(a+)` 匹配到 `a`，所以这时 `\1` 也是 `a`。

后面的 `aabaa` 同理。

如果一个正则有多个分组 `\2` `\3` 等同理。
